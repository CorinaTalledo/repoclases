import { extendTheme } from '@chakra-ui/react'

const theme = extendTheme({
    fonts: {
        text: `'Clock Alarm', sans-serif`,
        body: `'Body Font Name', sans-serif`,
    },
})

export default theme